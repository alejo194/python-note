```bash
def logger(fn):
    def wrap(*agrs,**dicts):##参数封装
        # before
        re = fn(*agrs,**dicts)##参数解构
        # after
        return re
    return wrap

def add(x,y):
    return x + y
    
foo = logger(add) ## 返回的是一个函数wrap
foo(1,2)

```
